<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper>
	<select id="select_officehr" resultType="com.mgrg.hrm.officehr.OhDTO">
		SELECT e.EMP_NAME, d.DEP_NAME, p.P_NAME, 
		
		 oh.W_START, oh.W_END
		FROM EMPLOYEES e, DEPARTMENT d, POSITIONRANK p, OFFICE_HOUR oh, HOLIDAY h 
		WHERE oh.EMP_UID = e.EMP_UID AND d.DEP_UID = e.dep_uid AND e.P_UID = p.P_UID 
		ORDER BY e.EMP_NAME ASC;
	</select>
</mapper>